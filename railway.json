{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd main && gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 120",
    "healthcheckPath": "/healthz",
    "healthcheckTimeout": 120
  }
}
